# AI Agent Directives for Eduard Bodak Design Architecture

## CRITICAL RULE: PERMANENT DESIGN SYSTEM
The design system, visual structure, fluid spacing (`--vw`), typography, pixel art glyphs, 3D card flipping, Lenis scrolling, Swup page transitions, and color palette (`#000` pitch black + `#111113` cards + `#00ff66` accents) are PERMANENT AND IMMUTABLE.

Any future edits must strictly preserve:
1. `public/showcase/styles.css`
2. `public/showcase/scripts/app.js`
3. `public/showcase/*.html`
4. `next.config.ts` route mappings
